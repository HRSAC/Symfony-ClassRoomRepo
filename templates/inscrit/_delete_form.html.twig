<form method="post" action="{{ path('app_inscrit_delete', {'id': inscrit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscrit.id) }}">
    <button class="btn">Delete</button>
</form>
