{% extends 'base.html.twig' %}

{% block title %}Inscrit{% endblock %}

{% block body %}
    <h1>Inscrit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscrit.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscrit_index') }}">back to list</a>

    <a href="{{ path('app_inscrit_edit', {'id': inscrit.id}) }}">edit</a>

    {{ include('inscrit/_delete_form.html.twig') }}
{% endblock %}
